import { createConnection } from 'typeorm';

/**
 * @description This method when started will be responsible for creating our project structure and find the ormconfig.json file and from it create our connection*/
createConnection();
